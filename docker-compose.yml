# docker-compose.yml
version: "3.8"

services:
  guestbook-api:
    build: .
    container_name: yapeng-guestbook
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - ADMIN_KEY=${ADMIN_KEY:-admin123}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
